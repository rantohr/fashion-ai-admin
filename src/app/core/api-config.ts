// Hardcoded for local dev - promote to an environment file once deployment
// is actually on the table (plan §2: "no deployment commitment in this plan").
export const API_BASE_URL = 'http://localhost:3000';
